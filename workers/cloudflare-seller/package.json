{
  "name": "@underwrite/cloudflare-seller",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "dry-run": "wrangler deploy --dry-run",
    "types": "wrangler types",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "smoke": "node scripts/smoke.mjs",
    "register": "node scripts/register-webhook.mjs"
  },
  "dependencies": {
    "@pdf-lib/fontkit": "^1.1.1",
    "agents": "^0.24.0",
    "fflate": "^0.8.3",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "@types/node": "^20.19.0",
    "typescript": "^5.9.2",
    "vitest": "^5.0.1",
    "wrangler": "^4.135.0"
  }
}
